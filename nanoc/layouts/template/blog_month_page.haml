%h1
  = "'#{year}-#{month}' Articles"

.month-page
  %ul
    - blog_items_in(year, month).each do |item|
      - url= url_for_item(item)
      %li
        %a(href="#{url}" title="#{item[:title]}")= item[:title]
