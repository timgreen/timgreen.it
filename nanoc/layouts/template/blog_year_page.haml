%h1
  = "'#{year}' Articles"

.year-page
  %ul
    - blog_month_items_in(year).each do |item|
      - url= url_for_item(item)
      %li
        %a(href="#{url}" title="#{item[:title]}")= item[:title]
